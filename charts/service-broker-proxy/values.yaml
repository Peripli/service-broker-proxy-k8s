# Default values for service-broker-proxy.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: cp-enablement.docker.repositories.sap.ondemand.com
  tag: latest
  pullPolicy: Always

service:
  type: ClusterIP
  port: 80

config:
  serviceManager:
    host: https://service-manager-core-test.cfapps.eu10.hana.ondemand.com
    user: admin
    password: admin
