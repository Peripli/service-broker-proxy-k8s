---
apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: service-broker-proxy
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: service-broker-proxy
    spec:
      containers:
      - name: service-broker-proxy
        image: ${REPOSITORY}/${PROJECT}:${VERSION}
        ports:
        - containerPort: 8080
      imagePullSecrets:
      - name: ${DOCKER_IMAGE_PULL_SECRET}
      volumes:
      - name: service-manager-secrets
        secret:
          secretName: ${SERVICE_MANAGER_SECRET}
