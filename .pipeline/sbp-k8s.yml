# Project Setup
general:
  githubApiUrl: 'https://api.github.com'
  githubServerUrl: 'https://github.com'
  gitSshKeyCredentialsId: 'alex-github-ssh'
  githubTokenCredentialsId: 'alex-public-github-token'

steps:
  pollBuilds:
    pollIntervalSeconds: 120
  pushChangesToRepo:
    gitUser: 'Alexander Penev'
    gitEmail: 'alexander.penev@sap.com'  
    githubApprovalTokenCredentialsId: 'peripli-github-token-approval'
  updatePackageRefs:
    deps: ['github.com/Peripli/service-broker-proxy']
  setupPipelineEnvironment:
    gitSshUrl: 'git@github.com:Peripli/service-broker-proxy-k8s.git'    